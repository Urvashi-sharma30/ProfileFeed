/**
 * ApiService - placeholder for API calls
 * Example: getFeed, getProfile, etc.
 */

export const ApiService = {
  async getFeed() {
    // replace with real endpoint
    // const res = await fetch('https://api.example.com/feed');
    // return await res.json();
    return [];
  }
};
