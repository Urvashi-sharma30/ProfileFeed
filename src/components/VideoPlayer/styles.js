import { StyleSheet } from "react-native";

export default StyleSheet.create({
  wrapper: { width: "100%", height: 220, backgroundColor: "#000", overflow: "hidden" },
  video: { ...StyleSheet.absoluteFillObject }
});
